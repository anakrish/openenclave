// Copyright (c) Open Enclave SDK contributors.
// Licensed under the MIT License.

enclave {
    from "openenclave/edl/attestation.edl" import *;
    from "openenclave/edl/logging.edl" import *;
    from "openenclave/edl/fcntl.edl" import *;
#ifdef OE_SGX
    from "openenclave/edl/sgx/platform.edl" import *;
#else
    from "openenclave/edl/optee/platform.edl" import *;
#endif

    trusted {
        public oe_result_t get_tls_cert_signed_with_ec_key(
            [out] unsigned char** data,
            [out] size_t* data_size);

        public oe_result_t get_tls_cert_signed_with_rsa_key(
            [out] unsigned char** data,
            [out] size_t* data_size);


	public void set_tmp_file([in, string] const char* path);
	
	public void print_bio_certificate([in, count=cert_length] const uint8_t* cert,
	                                  size_t cert_length);

        public void print_pem_certificate([in, count=cert_length] const uint8_t* cert,
	                                  size_t cert_length);

        public void print_pem_crl([in, count=crl_length] const uint8_t* crl,
	                          size_t crl_length);
    };
};
